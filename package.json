{
  "name": "tools",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./",
    "update-github-page": "parcel build --public-url ./ --dist-dir docs --no-source-maps",
    "clean": "rm -rf dist",
    "cleaner": "rm -rf dist node_modules .parcel-cache"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3",
    "preact": "^10.6.4",
    "preact-render-to-string": "^5.1.19",
    "preact-router": "^3.2.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "^2.0.1",
    "typescript": "^4.5.4"
  }
}
